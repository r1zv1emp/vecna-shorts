app = "vecna-shorts-bot"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"

# No HTTP service — this is a background worker
[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 256

[processes]
  worker = "node dist/index.js"
